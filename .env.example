# Server Configuration
PORT=3000
NODE_ENV=production

# Admin API Key (only required env var)
ADMIN_API_KEY=your_secure_random_key

# Optional: CORS restriction
# CORS_ORIGIN=https://yourdomain.com

# Note: Email is sent manually by Ares from his side
# No Gmail config needed on Render